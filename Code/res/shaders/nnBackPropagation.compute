#version 330 core
layout(local_size_x = 10, local_size_y = 1, local_size_z = 1) in;
// layout(std430, binding = 1) buffer weights
// {
//     unsigned int data[10];
// };

// uniform uWeights
// {
//     unsigned int weights[10];
// };

void main()
{
    //data[gl_LocalInvocationID] = gl_LocalInvocationID;
};